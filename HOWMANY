#include <stdio.h>
int main()
{
int N;
  scanf("%d",&N);
  if (N/10 < 1){
      printf("1");
}
  else if(N/10 < 10){
      printf("2");
  }
  else if(N/10 < 100){
      printf("3");
  }
  else{
      printf("More than 3 digits\n");
  }
  return 0;
}
